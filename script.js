// import Sortable from 'sortablejs';

Sortable.create(list, {
    animation: 150,
    // sort: true
    ghostClass: 'ghost'
});

// new Sortable(list, {
//     group: "sorting",
//     sort: true
// });